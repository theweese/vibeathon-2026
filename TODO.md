# Friday Night Vibeathon Prep

- [x] Create a new **Public** repository on GitHub (e.g., `st-joe-vibe-2026`).
- [ ] Push local code to the new repository *(User must push manually)*.
- [ ] Run `npm install` in the `app/` folder.
- [ ] Run `npm install lucide-react` in the `app/` folder.
- [ ] Run `npm run build` in the `app/` folder.
- [ ] Link the GitHub repo to Vercel/Netlify for auto-deployment.
