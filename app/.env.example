# Copy to .env.local and fill in. Never commit .env.local.

# OpenRouter API (for AI features in your app - if using client-side, use a proxy to keep key server-side)
# OPENROUTER_API_KEY=sk-or-v1-...
